services:
  postgres:
    image: postgres:16
    container_name: nspv-postgres
    environment:
      POSTGRES_DB: nspv
      POSTGRES_USER: nspv
      POSTGRES_PASSWORD: nspv
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

